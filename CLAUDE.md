# CLAUDE.md

Claude Code should follow the repository guidance in [`AGENTS.md`](AGENTS.md).

That file is the canonical development manual for this project. Keep this pointer instead of duplicating architecture, commands, deployment details, and persistence rules in two places.
